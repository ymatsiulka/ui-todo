{
  "name": "todo",
  "version": "1.0.0",
  "description": "React application to make request to create todo cards",
  "scripts": {
    "build": "if-env TYPE=api && (cd api && npm i) || (cd ui && npm i)",
    "api": "cd api && npm run dev",
    "ui": "cd ui && npm start",
    "start": "start npm run ui && start npm run api"
  },
  "dependencies": {
    "if-env": "^1.0.4"
  },
  "devDependencies": {
    "classnames": "^2.3.2"
  }
}
