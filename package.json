{
  "name": "todo",
  "version": "1.0.0",
  "description": "React application to make request to create todo cards",
  "scripts": {
    "build": "if-env TYPE=ui && (cd ui && npm i)",
    "ui": "cd ui && npm start",
    "start": "start npm run ui"
  },
  "dependencies": {
    "if-env": "^1.0.4"
  },
  "devDependencies": {
    "classnames": "^2.3.2"
  }
}
